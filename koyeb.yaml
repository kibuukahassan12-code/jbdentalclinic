services:
  - name: jb-dental-clinic
    type: web
    ports:
      - port: 8000
        protocol: http
    routes:
      - path: /
    healthcheck:
      path: /
      port: 8000
    build:
      command: npm ci && npm run build
    run:
      command: npm start
